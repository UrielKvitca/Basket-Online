{
  "name": "basket-random-arena",
  "version": "1.0.0",
  "private": true,
  "description": "Juego arcade de basquet 2v2 con fisicas random, online, tienda y torneo.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/js/engine.js && node --check public/js/storage.js && node --check public/js/audio.js && node --check public/js/network.js && node --check public/js/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1"
  }
}
